package main

import (
	"fmt"
	"math"
	//"math/rand"
	//"time"
)

func main(){
	// rand.Seed(time.Now().UnixNano()) //refreshes random number outputs every time the program is run
	//fmt.Println("My favourite number is", rand.Intn(10)) //prints out a random number to the console
	fmt.Println(math.Pi) // prints out the value of Pi
}